"""
Put as any. If I put them as abstract classes with the methods that I need,
type hinting will 'help'. I don't use the actual sage types because it
goes in a factory.
"""

import typing

type PolyMat = typing.Any
type PolyVec = typing.Any
type Poly = typing.Any
